name: asset_plus
description: An UI assignment for Asset_Plus

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  http: ^1.5.0
  provider: ^6.1.5
  google_fonts: ^8.0.2
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dependency_overrides:
  path_provider: ^2.1.5
  path_provider_android: ^2.2.17
  path_provider_windows: ^2.3.0
  win32: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true
